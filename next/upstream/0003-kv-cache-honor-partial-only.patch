diff --git a/src/llama-kv-cache.cpp b/src/llama-kv-cache.cpp
index f22054c..5982528 100644
--- a/src/llama-kv-cache.cpp
+++ b/src/llama-kv-cache.cpp
@@ -1241,12 +1241,24 @@ ggml_tensor * llama_kv_cache::get_k_storage(int32_t il) const {
     return layers[ikv].k;
 }
 
+ggml_tensor * llama_kv_cache::get_v_storage(int32_t il) const {
+    const int32_t ikv = map_layer_ids.at(il);
+
+    return layers[ikv].v;
+}
+
 const llama_kv_cells & llama_kv_cache::get_cells(llama_seq_id seq_id) const {
     GGML_ASSERT(seq_id >= 0 && (size_t) seq_id < seq_to_stream.size());
 
     return v_cells[seq_to_stream[seq_id]];
 }
 
+uint32_t llama_kv_cache::get_stream_of(llama_seq_id seq_id) const {
+    GGML_ASSERT(seq_id >= 0 && (size_t) seq_id < seq_to_stream.size());
+
+    return seq_to_stream[seq_id];
+}
+
 uint32_t llama_kv_cache::get_n_kv(const slot_info & sinfo) const {
     uint32_t result = 0;
 
@@ -2050,7 +2062,12 @@ void llama_kv_cache::state_write(llama_io_write_i & io, llama_seq_id seq_id, lla
         return;
     }
 
-    GGML_UNUSED(flags);
+    // NEXT: a full-attention KV cache is rolled back with seq_rm, so a partial (checkpoint) state carries nothing
+    // for it. Without this the server's context checkpoints copied the whole MTP draft cache (~2 KB/token, hundreds
+    // of MB at long context) to the host on every request.
+    if ((flags & LLAMA_STATE_SEQ_FLAGS_PARTIAL_ONLY) && !hparams.is_swa_any()) {
+        return;
+    }
 
     io.write(&n_stream, sizeof(n_stream));
 
@@ -2129,7 +2146,10 @@ const slot_info_vec_t *   sinfos_in) {
         return;
     }
 
-    GGML_UNUSED(flags);
+    // NEXT: mirrors state_write - a partial checkpoint holds nothing for a full-attention cache
+    if ((flags & LLAMA_STATE_SEQ_FLAGS_PARTIAL_ONLY) && !hparams.is_swa_any()) {
+        return;
+    }
 
     // TODO: fix incosistent handling of `seq_id < 0` and `seq_id == -1` in the codebase [TAG_LLAMA_SEQ_ID_NEG]
     GGML_ASSERT(seq_id == -1 || (seq_id >= 0 && (size_t) seq_id < seq_to_stream.size()));
