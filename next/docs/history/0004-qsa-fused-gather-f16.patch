--- /home/douya/tests/opt-20260913/orig/ggml/src/ggml-qsa.c	2026-09-13 09:58:35.919736050 +0800
+++ ggml/src/ggml-qsa.c	2026-09-13 18:49:38.090445593 +0800
@@ -23,6 +23,27 @@
             const float inv = 1.0f/sqrtf(ss/128 + ggml_qsa_epsilon(dst));
             for (int d = 0; d < 128; ++d) out[b*128+d] = (x[d]*inv)*gamma[d];
         }
+    } else if (kind == 4) {
+        // dst [d, np, nh, nq] F16; src[0] cache [d, nh, nk] Q8_0 rows; src[1] ids [ns, nq]
+        const struct ggml_tensor * indices = dst->src[1];
+        const int64_t d = dst->ne[0], np = dst->ne[1], nh = dst->ne[2], ns = indices->ne[0];
+        ggml_fp16_t * result = dst->data;
+        float row[4096];
+        GGML_ASSERT(d*nh <= 4096);
+        for (int64_t q = ith; q < dst->ne[3]; q += nth) {
+            const int32_t * sel = (const int32_t *)((const char *)indices->data + q*indices->nb[1]);
+            for (int64_t s = 0; s < np; ++s) {
+                if (s < ns) {
+                    dequantize_row_q8_0((const block_q8_0 *)((const char *)a->data + (size_t)sel[s]*a->nb[2]), row, d*nh);
+                }
+                for (int64_t h = 0; h < nh; ++h) {
+                    ggml_fp16_t * o = result + d*(s + np*(h + nh*q));
+                    for (int64_t i = 0; i < d; ++i) {
+                        o[i] = s < ns ? ggml_fp32_to_fp16(row[h*d + i]) : 0;
+                    }
+                }
+            }
+        }
     } else if (kind == 3) {
         ggml_fp16_t * result = dst->data;
         const struct ggml_tensor * indices = dst->src[1];
@@ -78,3 +99,13 @@
     struct ggml_tensor * args[] = {mask,ids};
     return ggml_custom_4d(ctx,GGML_TYPE_F16,padded,1,1,ids->ne[1],args,2,qsa_cpu,GGML_N_TASKS_MAX,(void *)(intptr_t)3);
 }
+
+// Fused get_rows + pad + cast for the QSA compact path: one pass instead of three.
+GGML_API struct ggml_tensor * ggml_qsa_gather_f16(struct ggml_context * ctx, struct ggml_tensor * cache, struct ggml_tensor * ids, int64_t padded) {
+    GGML_ASSERT(cache->type == GGML_TYPE_Q8_0 && cache->ne[0] % 32 == 0 && cache->ne[3] == 1);
+    GGML_ASSERT(cache->nb[1] == ggml_row_size(cache->type, cache->ne[0]) && cache->nb[2] == cache->ne[1]*cache->nb[1]);
+    GGML_ASSERT(ids->type == GGML_TYPE_I32 && ggml_is_contiguous(ids) && ids->ne[2] == 1 && ids->ne[3] == 1);
+    GGML_ASSERT(padded >= ids->ne[0] && padded % 256 == 0);
+    struct ggml_tensor * args[] = {cache, ids};
+    return ggml_custom_4d(ctx, GGML_TYPE_F16, cache->ne[0], padded, cache->ne[1], ids->ne[1], args, 2, qsa_cpu, GGML_N_TASKS_MAX, (void *)(intptr_t)4);
+}
--- /home/douya/tests/opt-20260913/orig/ggml/src/ggml-cuda/qsa.cu	2026-09-13 09:58:35.921377780 +0800
+++ ggml/src/ggml-cuda/qsa.cu	2026-09-13 18:49:38.091824684 +0800
@@ -43,6 +43,33 @@
         out[q*np+j] = j < ns ? m[row[j]] : __float2half(-INFINITY);
     }
 }
+static __global__ void qsa_gather_f16(const char * cache, const char * ids, half * out,
+        int64_t d, int64_t nh, int64_t ns, int64_t np, size_t cell_stride, size_t ids_stride) {
+    const int64_t s = int64_t(blockIdx.x)*blockDim.x + threadIdx.x;
+    const int64_t q = blockIdx.y;
+    if (s >= np) {
+        return;
+    }
+    if (s >= ns) {
+        for (int64_t h = 0; h < nh; ++h) {
+            half * o = out + d*(s + np*(h + nh*q));
+            for (int64_t i = 0; i < d; ++i) {
+                o[i] = __float2half(0.0f);
+            }
+        }
+        return;
+    }
+    const int32_t cell = reinterpret_cast<const int32_t *>(ids + q*ids_stride)[s];
+    const char * row = cache + size_t(cell)*cell_stride;
+    for (int64_t h = 0; h < nh; ++h) {
+        half * o = out + d*(s + np*(h + nh*q));
+        for (int64_t i = 0; i < d; ++i) {
+            const int64_t g = h*d + i;
+            const char * block = row + (g/32)*34;
+            o[i] = __float2half(__half2float(*reinterpret_cast<const half *>(block)) * float(reinterpret_cast<const int8_t *>(block + 2)[g % 32]));
+        }
+    }
+}
 void ggml_cuda_op_qsa(ggml_backend_cuda_context & ctx, ggml_tensor * dst) {
     const auto * a = dst->src[0];
     const auto * ids = dst->src[1];
@@ -50,6 +77,9 @@
     if (ggml_qsa_kind(dst) == 1) {
         qsa_pool_norm<<<dst->ne[1],128,0,ctx.stream()>>>((const char *)a->data,(const int32_t *)ids->data,
             (const float *)c->data,(float *)dst->data,a->nb[1],ggml_qsa_epsilon(dst));
+    } else if (ggml_qsa_kind(dst) == 4) {
+        qsa_gather_f16<<<dim3((dst->ne[1]+255)/256,dst->ne[3]),256,0,ctx.stream()>>>((const char *)a->data,
+            (const char *)ids->data,(half *)dst->data,dst->ne[0],dst->ne[2],ids->ne[0],dst->ne[1],a->nb[2],ids->nb[1]);
     } else if (ggml_qsa_kind(dst) == 3) {
         qsa_mask_select<<<dim3((dst->ne[0]+255)/256,dst->ne[3]),256,0,ctx.stream()>>>((const char *)a->data,
             (const char *)ids->data,(half *)dst->data,ids->ne[0],dst->ne[0],a->nb[1],ids->nb[1]);
