# Optimized Next deployment in progress

Latest user authorizes deploying optimized Next once, real 262K measurements and profiling. Next is preferred for quality; do not switch back to Huihui merely because slower. Previous no-load constraint has been lifted by latest user.

Service qwen38-flashnext-optimized-262k.service started at 2026-09-13 08:12:17, PID in server.pid (214161 initially). User douya, localhost8093, original 3-card 6/22/21 split, Q8KV, ctx262144, MTP3. Config launch.json, wrapper run-server.py. Huihui stopped after idle metrics confirmed. New unit active, not enabled for boot. Model currently loading from slow HDD at60-70MB/s; about14GB read at08:15. Do not count load as decode. No driver/clock/reboot changes.

Source /home/douya/src/llama.cpp-flashnext-20260913, base d1a9235, existing IQ4NL getrows and no-mmap-prefetch patches retained. New custom GGML operators in ggml/src/ggml-qsa.c/.h (CPU callbacks) and ggml-cuda/qsa.cu/.cuh (CUDA). Recognition via ggml_qsa_kind on CUSTOM op; CPU/GPU supported, no enum change. qwen4exp.cpp integrates fused pool/norm, fused expand, rowwise TopK, compact selected-KV attention. CPU fast path in llama-memory-hybrid-idx.cpp. Fattn.cu selects MMA for SM80 Q8 head256, ratio12, singlequery longctx. All guarded by NEXT_QSA_OPT and longctx>=131072, <=8query where relevant; original graph paths remain for prefill/other shapes.

Build target llama succeeded. Integrated pool max error7.2e-7, expand exact for1/4/8query, integrated CPU arrays exact on4tail cases (~0.64ms), MTP real singlequery MMA ~1.20ms vs duplicatedquery1.19ms. Baseline copied to baseline-bin (~223MB); use LD_LIBRARY_PATH=baseline-bin when running it because binaries have build RPATHs.

Profiling: libnext-trace.so is preloaded into service, inactive until profile.flag exists. CUDA12.4 CUPTI Activity captures CONCURRENT_KERNEL, MEMCPY, RUNTIME to profile.csv, max15sec/capture. Disable/remove flag flushes. CSV includes CPU scopes DECODE_TARGET, DECODE_MTP, QSA_CPU_INPUT via weak hooks in llama-context.cpp and llama-memory-hybrid-idx.cpp. Late enabling after graph creation was tested successfully:142/142graph kernels matched runtime correlation IDs. No Nsight importer needed. Do not overwrite loaded libnext-trace.so.

bench.py supports --profile; touches profile.flag only on first generated token and removes at end, saves token events. Normal benchmark first (short smoke optional), then context261888/tokens256. Repeat samefullprompt should reuse near-full prefix. Compare normal results to prior Next short65.2,64K40.3,full18.3479. Original baseline full tokens/needle outputs in parent directory. Separate profiled throughput from normal. analyze-profile.py consumes CSV, groups kernels by role via runtimecorrelation and CPU decode spans; reports CPU inclusive durations, CUDA APIs, memcpy and device busy unions. Do not sum inclusive CPU/GPU times as latency.

Need: finish load, health/maxctx verify, normal short/262K tests and correctness; then profile repeated262K generation; analyze bottlenecks and actual improvement. Keep Next running after success. Capture source bundle including untracked new files (git diff alone excludes them), hashes, logs and report. Background benchmark watcher is temporary and should stop via stop-background-watch marker after tests.
